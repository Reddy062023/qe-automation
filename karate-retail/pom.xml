<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!--
        WHAT IS pom.xml?
        pom.xml = Project Object Model
        Heart of every Maven project.
        Like package.json in Node.js.
        Defines dependencies, build config, plugins.
    -->

    <groupId>com.retailshop</groupId>
    <artifactId>karate-retail</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>Karate Retail API Tests</name>
    <description>AI-powered Karate API test suite for RetailShop API</description>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Library versions - change here only -->
        <karate.version>1.4.0</karate.version>
        <junit5.version>5.10.0</junit5.version>
        <allure.version>2.24.0</allure.version>
    </properties>

    <dependencies>

        <!--
            KARATE - main API testing framework
            Includes HTTP client, JSON parser, assertions, Gherkin
            No extra libraries needed for API testing
        -->
        <dependency>
            <groupId>com.intuit.karate</groupId>
            <artifactId>karate-junit5</artifactId>
            <version>${karate.version}</version>
            <scope>test</scope>
        </dependency>

        <!--
            JUNIT 5 - test runner
            Karate uses JUnit 5 to discover and run feature files
        -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit5.version}</version>
            <scope>test</scope>
        </dependency>

        <!--
            ALLURE JUNIT5 - professional reporting
            Captures test results in Allure format
            Then: mvn allure:serve to open visual dashboard
            Same Allure report we used with Playwright in Phase 2
        -->
        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-junit5</artifactId>
            <version>${allure.version}</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <!--
                MAVEN SUREFIRE PLUGIN
                Runs JUnit 5 tests via mvn test
                Configured to generate Allure results
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.1.2</version>
                <configuration>
                    <useFile>false</useFile>
                    <!-- Tell Allure where to save results -->
                    <systemPropertyVariables>
                        <allure.results.directory>
                            ${project.build.directory}/allure-results
                        </allure.results.directory>
                    </systemPropertyVariables>
                </configuration>
            </plugin>

            <!--
                ALLURE MAVEN PLUGIN
                Generates and serves the Allure HTML report
                Run: mvn allure:serve    to open in browser
                Run: mvn allure:report   to generate HTML only
            -->
            <plugin>
                <groupId>io.qameta.allure</groupId>
                <artifactId>allure-maven</artifactId>
                <version>2.12.0</version>
                <configuration>
                    <reportVersion>${allure.version}</reportVersion>
                    <resultsDirectory>
                        ${project.build.directory}/allure-results
                    </resultsDirectory>
                </configuration>
            </plugin>

            <!--
                MAVEN COMPILER PLUGIN
                Compiles Java code with Java 17
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                </configuration>
            </plugin>

        </plugins>

        <!--
            TEST RESOURCES
            Tells Maven where to find .feature files
            src/test/resources = .feature files and karate-config.js
        -->
        <testResources>
            <testResource>
                <directory>src/test/resources</directory>
            </testResource>
            <testResource>
                <directory>src/test/java</directory>
                <excludes>
                    <exclude>**/*.java</exclude>
                </excludes>
            </testResource>
        </testResources>

    </build>

</project>